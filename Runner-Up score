Given the participants' score sheet for your University Sports Day, you are required to find the runner-up score. You are given  scores. Store them in a list and find the score of the runner-up. runner up means the score just lower than the higher one 

#-----------------------CODE----------------------------#
n = int(input())
    arr = map(int, input().split())
    arr= list(set(arr))
    arr.sort()
    n= len(arr)
    print(arr[-2])
